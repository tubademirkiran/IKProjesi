@model IKProjesi.UI.Models.View.UnvanModel

@{
    ViewBag.Title = "Unvan";
    Layout = "~/Views/Shared/_LayoutPage1.cshtml";
}

<h2 style="text-align:center">Ünvan</h2>
<p></p>
<p>
    @Html.ActionLink("Yeni Kayıt Ekle", "Ekle")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.unvan.Id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.unvan.Ad)
        </th>
        <th>Güncelle</th>
        <th>Datay</th>
        <th>Sil</th>
    </tr>

    @foreach (var item in Model.list)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Ad)
            </td>
            <td>
                @Html.ActionLink("Güncelle", "Guncelle", new { id = item.Id })
            </td>
            <td>
                @Html.ActionLink("Detay", "Detay", new { id = item.Id })
            </td>
            <td>
                @Html.ActionLink("Sil", "Sil", new { id = item.Id })
            </td>
        </tr>
    }

</table>


